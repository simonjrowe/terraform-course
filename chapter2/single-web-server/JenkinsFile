#!/usr/bin/groovy

pipeline {
    agent any

    stages {

        stage ('Build') {
            steps { buildApp() }
        }
    }
}

// Steps
def buildApp() {
    dir ('chapter2\single-web-server' ) {
        bat "terraform apply"
    }
}